'use strict'

const Routes = require('@refkinscallv/express-routing')
// const UserController = require('@app/http/controllers/user.controller')

Routes.group('api', () => {
    // Routes.group('users', () => {
    //     Routes.get('get/:token', [UserController, 'get'])
    // })
})
